Once this repo is cloned to the home directory on a new machine, vim should be 
properly configured. Older vim versions might require you to copy the .vimrc 
file in this directory to your home directory.
